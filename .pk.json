{
  "changelog": {
    "hooks": {
      "preCommit": "pnpm -r exec npm version ${VERSION#v} --no-git-tag-version && git add 'packages/**/package.json' pnpm-lock.yaml"
    }
  },
  "guard": {
    "branches": ["main"]
  },
  "release": {
    "branch": "main",
    "hooks": {
      "preRelease": "pnpm install --frozen-lockfile && pnpm -r run --if-present lint && pnpm -r run --if-present test && pnpm -r run --if-present build"
    }
  }
}
